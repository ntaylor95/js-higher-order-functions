const isPrime = (value) => {// enter your code here
var lowestFactor;
var isPrime = false;

//check if number is 1
if (value === 1) return 1;

// divide number by 2 and check for a remainder
const remainder = value % 2;
console.log(remainder);

// if not prime, return lowest factor found
return lowestFactor;

// is a prime number
return 1;
}

isPrime(32);
isPrime(37);